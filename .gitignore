# Skill bundles installed via `npx skills add`.
# Tracked by skills-lock.json; re-install with `npx skills install`.
.agents/skills/stripe-best-practices/
.agents/skills/stripe-projects/
.agents/skills/upgrade-stripe/
.claude/skills/stripe-best-practices
.claude/skills/stripe-projects
.claude/skills/upgrade-stripe

# Local TODO scratchpads
stripe-todo.txt

# Mobile-design verification screenshots. Folder kept via .gitkeep so the
# documented save location stays stable, but the files themselves are
# ephemeral inspection artifacts and shouldn't bloat the repo.
.mobile-audit/*
!.mobile-audit/.gitkeep
